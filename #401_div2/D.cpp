#include <bits/stdc++.h>
#define pii pair<int, int>
using namespace std;
const int maxn = 5e5 + 10;
string s[maxn];
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin >> n;
    for(int i = 0; i < n; i++)
        cin >> s[i];
    
    return 0;
}
