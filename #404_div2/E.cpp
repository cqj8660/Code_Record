#include <bits/stdc++.h>
#define vi vector<int>
using namespace std;
const int maxn = 4e5 + 10;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, q;
    cin >> n >> q;
    while(q--)
    {
        int l, r;
        cin >> l >> r;

    }
    return 0;
}
