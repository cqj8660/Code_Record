#include <bits/stdc++.h>
#define ll long long
const int maxn = 2e5 + 10;
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    bitset<30> q(998244353);
    for(int i = 0; i < 20; i++)
        cout << q[i];
    int n, m;
    cin >> n >> m;
    string a, b;
    cin >> a >> b;

    return 0;
}
