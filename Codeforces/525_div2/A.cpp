#include <bits/stdc++.h>
#define ll long long
#define pii pair<int, int>
using namespace std;
pii a[2000];
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int x;
    cin >> x;
    if(x == 1)
        cout << -1 << endl;
    else
        cout << x << ' ' << x << endl;
    return 0;
}
