#include <iostream>
#include <set>
#include <cstring>
#include <vector>
#include <map>
#include <cmath>
#include <algorithm>
#include <string>
#include <queue>
#include <set>
#include <cctype>
#include <unordered_map>
#define ll long long
using namespace std;
const int maxn = 1e3 + 10;
map<int, int> g[maxn];
int a[maxn];
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int m, n;
    cin >> m >> n;

    return 0;
}
